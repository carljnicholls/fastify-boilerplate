info:
  name: teapot
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8080/api/v1/health/teapot
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("teapot status", function () {
          expect(res.getStatus()).to.equal(418);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
