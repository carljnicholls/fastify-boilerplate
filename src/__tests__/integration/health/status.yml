info:
  name: status
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8080/api/v1/health/status
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
