info:
  name: 2 get user by id
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8080/api/v1/users/user_id
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const user = JSON.parse(bru.getVar("user"));

        const url = 'http://127.0.0.1:8080/api/v1/users/' + user.user_id

        // console.log(user, url)

        req.setUrl(url)
    - type: tests
      code: |-
        test("should match expected value", function () {
          const expected = JSON.parse(bru.getVar("user"));
          const result = res.getBody()
        //   console.warn('2 get user', {
        //     e: (user), 
        //     r: (result)
        //   })
          expect(expected).to.deep.equal(result);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
