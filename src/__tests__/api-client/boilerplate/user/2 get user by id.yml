info:
  name: 2 get user by id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/user/user_id
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const user = JSON.parse(bru.getVar("user"));

        const url = 'http://localhost:8080/api/v1/user/' + user.user_id

        // console.log(user, url)

        req.setUrl(url)
    - type: tests
      code: |-
        test("should match expected value", function () {
          const user = JSON.parse(bru.getVar("user"));
          
          expect(JSON.stringify(res.getBody())).to.equal(JSON.stringify(user));
          
          
                 
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
